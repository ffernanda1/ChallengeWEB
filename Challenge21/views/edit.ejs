<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Data</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">

    <script>
        function Previous() {
            window.history.back()
        }
    </script>


</head>

<body>

    <div class="container my-5">
        <h1>Edit Data</h1>
        <div class="bg-light p-5 rounded">
            <form action="" method="post">

                <div class="row g-3 align-items-center">
                    <div class="mb-5 row">
                        <label for="ids" class="col-sm-1 col-form-label">id</label>
                        <div class="col-sm-5">
                            <input type="number" class="form-control" name="ids" value="<%= item.id %>">
                        </div>
                    </div>
                </div>

                <div class="row g-3 align-items-center">
                    <div class="mb-5 row">
                        <label for="String" class="col-sm-1 col-form-label">String</label>
                        <div class="col-sm-5">
                            <input type="text" class="form-control" name="Strings" value="<%= item.strings %>">
                        </div>
                    </div>
                </div>

                <div class="row g-3 align-items-center">
                    <div class="mb-5 row">
                        <label for="Integer" class="col-sm-1 col-form-label">Integer</label>
                        <div class="col-sm-5">
                            <input type="text" class="form-control" name="Integers" value="<%= item.integers %>">
                        </div>
                    </div>
                </div>

                <div class="row g-3 align-items-center">
                    <div class="mb-5 row">
                        <label for="Float" class="col-sm-1 col-form-label">Float</label>
                        <div class="col-sm-5">
                            <input type="text" class="form-control" step=0.01 name="Floats" value="<%= item.floats %>">
                        </div>
                    </div>
                </div>


                <div class="row g-3 align-items-center">
                    <div class="mb-5 row">
                        <label for="Date" class="col-sm-1 col-form-label">Date</label>
                        <div class="col-sm-5">
                            <input type="date" class="form-control" name="Dates"
                                value="<%= moment(item.dates).format(`YYYY-MM-DD`) %>">
                        </div>
                    </div>
                </div>

                <div class="row g-3 align-items-center">
                    <div class="mb-5 row">
                        <label for="Booleans" class="col-sm-1 col-form-label">Boolean</label>
                        <div class="col-sm-5">
                            <select class="form-select" name="Booleans" id="Booleans">
                                <option disabled hidden>Choose the boolean..</option>
                                <option value="true" <%=item.booleans==true ? 'selected' : null %> >true</option>
                                <option value="false" <%=item.booleans==false ? 'selected' : null %>>false</option>
                            </select>
                        </div>
                    </div>
                </div>
                <br>
                <input class="btn btn-success" type="submit" value="Save">
                <input class="btn-btn-warning" type="button" value="Back" onclick="Previous()">
            </form>
        </div>
    </div>
</body>

</html>